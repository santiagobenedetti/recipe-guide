const { Router} = require("express");
const router = Router();



router.post('/register', async (req, res) => {
  // TODO -> Create the user coming from the form into the db if not exists
})

router.post('/login', async (req, res) => {

})

export default router;